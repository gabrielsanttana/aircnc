import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 60,
    },

    logo: {
        alignSelf: "center",
        resizeMode: "contain",
        height: 32
    }
});

export default styles;